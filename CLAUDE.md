# Claude Code 向けの案内

このリポジトリで作業するときは、まず `docs/development-notes.md` を読んでください。
設計の理由と、触るときの注意（特にQR形式と印刷まわり）が書いてあります。

## 構成

- `src/App.jsx` … 患者用アプリ（医療者モードと受付端末モードを内蔵）。約4,400行
- `src/Terminal.jsx` … 受付端末（血圧・心不全の両方に対応）。約1,600行
- `src/HeartFailure.jsx` … 心不全手帳（**開発中・未配布**）。約3,900行。開発メモ第11節を参照
- `src/base.css` … レイアウト用の最小限のクラスと印刷指定。Tailwindは使っていない
- `docs/` … 配布物（HTMLを印刷してA4で使う）と、この開発メモ
- `public/` … アイコンとマニフェスト

## よく使うコマンド

```bash
npm install
npm run dev      # http://localhost:5173
npm run build    # dist/ に出力
```

## 守ってほしいこと

- **依存を増やさない。** QRの生成・読み取り、地図はすべて自前。通信は起動時のみという状態を保つ
- **QR形式を変えたら受付端末を先に更新する。** 既存の `FMT_SPECS` は消さず、番号を足す
- **患者データをサーバに送らない。** 記録は端末内だけ、という前提で全体が組まれている
- **アプリは診断しない。** 受診の目安まで。不整脈マークは表示のみで判定しない
- **成育医療研究センターの表は転載しない。** 要約とリンクにとどめる
- 印刷の不具合は原因が分かりにくい。`docs/development-notes.md` の第8節に既知の症状と対処がある
- **心不全手帳には印刷の修正・メモ欄・暗号化が未反映。** 触る前に第11節を読むこと

## 変更したあと

```bash
npm run build    # 通ることを確認
git add . && git commit -m "説明" && git push
```

push すると Vercel が自動で反映します（2〜3分）。公開先は https://shiga-bp-note.vercel.app/
